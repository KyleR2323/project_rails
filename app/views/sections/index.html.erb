<% content_for(:html_title) {"Sections"}%>
<h2>Sections</h2>
<br>
<div class="columns is-multiline">
  <% @sections.each do |section| %>
  <div class="column is-one-quarter">
    <div>
      <%= link_to "#{section.name}", section %>
    </div>
  </div>
  <% end %>
</div>
