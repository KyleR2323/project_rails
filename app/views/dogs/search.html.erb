<p class="mb-5">
  Found <%= pluralize(@dogs.count, 'dog') %> when searching for: <%= params[:keywords] %>
</p>
<div class="columns is multiline">
  <% @dogs.each do |dog| %>
  <div class="column is-one-quarter">
    <div>
      <%= link_to "#{dog.name}", dog %>
    </div>
  </div>
  <% end %>
</div>